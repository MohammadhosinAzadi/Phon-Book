import { join } from 'path';

export const dataFolder = join(__dirname, '../../../data');
export const dataPath = join(dataFolder, 'phonebook.json');
export const categoryPath = join(dataFolder, 'categories.json');
